# Prompt Pack (MVC + SOLID + DDD)

## Nova feature

```text
Implemente a feature "<NOME_FEATURE>" usando MVC + SOLID + DDD.
1) modele dominio e invariantes;
2) crie/ajuste caso de uso;
3) exponha endpoint com DTOs;
4) mantenha infra desacoplada;
5) adicione testes unitarios e de integracao.
```

## Review arquitetural

```text
Revise este codigo com foco em MVC + SOLID + DDD.
Aponte violacoes de camada, acoplamentos indevidos e regras fora do dominio.
Retorne em ordem de severidade com sugestao de correcao.
```

## Refatoracao de dominio

```text
Refatore o fluxo para mover regra de negocio ao dominio/aplicacao, mantendo contrato externo e adicionando testes de regressao.
```
