# Contexto — API em camadas

- Use `package.json`, `tsconfig.json` e arquivos de roteamento para entender o layout.
- Anexe exemplos de `routes/`, `controllers/` e `services/` quando pedir mudanças.
- Inclua contratos de request/response e exemplos de payloads.
- Forneça o contexto de banco de dados, cache ou APIs externas usados no projeto.
