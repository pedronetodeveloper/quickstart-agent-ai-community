# Prompt pack — API em camadas

## Nova feature de endpoint

```text
Implemente o endpoint <MÉTODO> <PATH> seguindo API em camadas (rotas finas + serviço + repositório).
Stack: <FastAPI|Flask>. Inclua schemas na borda, type hints, tratamento de erro HTTP consistente e testes pytest.
```

## Revisão de camadas

```text
Revise este código com foco em separação api/services/infra. Liste violações por severidade e sugira correção objetiva.
```

## Refatorar rota gorda

```text
Extraia a lógica desta rota para um serviço testável, mantendo o contrato HTTP. Adicione testes unitários do serviço.
```
